import classNames from 'classnames';
import React from 'react';

function Messangers({ className }) {
    return (
        <div className={classNames('messangers', className)}>
            
        </div>
    );
}

export default Messangers;